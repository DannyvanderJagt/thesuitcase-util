{
  "name": "thesuitcase-util",
  "version": "0.0.1",
  "description": "An collection of regularly used mini libraries.",
  "main": "dist/index.js",
  "dependencies": {
    "cli-color": "^1.1.0",
    "moment": "^2.14.1"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.9.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && babel src --out-dir dist -w"
  },
  "author": "The Suitcase",
  "license": "MIT"
}
